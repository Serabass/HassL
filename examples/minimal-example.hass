// Минимальный пример - самая простая структура

home 'Minimal' {
  area 'Room' room {
    device 'Device' device {
      entities: [
        sensor value { id: 'sensor.value'; }
      ];
    }
  }
}

automation 'Minimal automation' {
  when room.device.value > 10 {
    call notify.telegram('Value is high');
  }
}
