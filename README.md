# HassLanguage - DSL для умного дома

DSL (Domain-Specific Language) для написания автоматизаций умного дома поверх Home Assistant. Язык с C-подобным синтаксисом, event-driven архитектурой и поддержкой неблокирующих операций.

## 🎯 Особенности

- **C-подобный синтаксис** со `{ }` блоками
- **Человекочитаемый** и интуитивный
- **Event-driven** архитектура (готово к WebSocket / RabbitMQ)
- **Неблокирующий** runtime (никаких sleep, wait реализован через state machine)
- **Условия с удержанием** (`for 30m` - условие должно быть истинным непрерывно)
- **Декораторы** для автоматизаций и условий (`@mode`, `@cooldown`, `@edge`, и т.д.)
- **Типизация** с поддержкой примитивов, строк, времени, диапазонов

## 📦 Структура проекта

```
HassLanguage/
├── src/
│   ├── HassLanguage.Core/        # AST модель, типы, валидация
│   ├── HassLanguage.Parser/       # Парсер на Sprache
│   ├── HassLanguage.Runtime/      # Runtime engine для выполнения
│   └── HassLanguage/              # Консольное приложение
├── examples/                      # Примеры .hass файлов
└── tests/                         # Unit тесты
```

## 🚀 Быстрый старт

### Сборка через Docker (рекомендуется)

```powershell
.\build.ps1
```

### Запуск примера

```powershell
.\run.ps1 examples/simple-example.hass
```

Или напрямую:

```powershell
docker-compose -f docker-compose.run.yml run --rm hass-language dotnet HassLanguage.dll examples/simple-example.hass
```

## 📝 Синтаксис

### Иерархия: home → area → device → entities

```c
home "MyFlat" {
  area "Kitchen" kitchen {
    device "Light" light {
      entities: [
        light ceiling { id: "light.kitchen_ceiling"; },
        light counter { id: "light.kitchen_counter"; }
      ];
    }

    device "Sensors" sensors {
      entities: [
        binary_sensor motion { id: "binary_sensor.kitchen_motion"; },
        sensor temp { id: "sensor.kitchen_temperature"; unit: "°C"; }
      ];
    }
  }
}
```

### Автоматизация

```c
automation "Kitchen motion light" {
  when sensors.motion == "on" {
    do light.turn_on(light.ceiling, { brightness: 70 });
    wait sensors.motion == "off" for 40s timeout 10m;
    do light.turn_off(light.ceiling);
  }
}
```

### Условия с удержанием

```c
automation "AC auto cool" {
  when living.climate.temp > 25.0 for 30m {
    do climate.set_mode(living.climate.ac, "cool");
  }
}
```

### Комбинированные условия

```c
// Все условия должны выполняться 20 минут
automation "Smart AC" {
  when all for 20m {
    living.sensors.temp > 25.0;
    now().time in 09:00..23:00;
    living.sensors.window == "closed";
  } {
    do climate.set_mode(living.climate.ac, "cool");
  }
}

// Любое из условий
automation "Alert" {
  when any {
    living.sensors.temp > 28.0;
    living.sensors.humidity > 70;
  } {
    do notify.telegram("Проветри!");
  }
}
```

### Диапазоны

```c
// Диапазон времени (поддерживает переход через полночь)
when now().time in 23:00..06:00 { ... }

// Диапазон значений
when living.sensors.temp in 20.0..24.0 for 15m { ... }
```

### Декораторы

```c
@mode(restart)
@cooldown(10s)
@lock("bath_light", 2s)
automation "Bathroom light" {
  @edge(rising)
  @debounce(2s)
  @max_age(5m)
  when bathroom.sensors.motion == "on" {
    do light.turn_on(bathroom.light.main, { brightness: 100 });
  }
}
```

## ✅ Что реализовано

### Парсер (Sprache)
- ✅ Полный парсинг синтаксиса DSL
- ✅ Поддержка home/area/device/entity иерархии
- ✅ Поддержка automation/when/do/wait конструкций
- ✅ Декораторы для automation и when
- ✅ Выражения с приоритетами операторов
- ✅ Литералы: bool, int, float, string, Duration, TimeOfDay, DateTime
- ✅ Диапазоны (Range<T>)
- ✅ Функции и ссылки на entities

### AST модель
- ✅ Полная модель AST для всех конструкций языка
- ✅ Типы данных: Duration, TimeOfDay, Range
- ✅ Выражения, условия, действия
- ✅ Декораторы с аргументами

### Семантическая валидация
- ✅ Проверка уникальности alias на всех уровнях
- ✅ Проверка корректности ссылок на entities
- ✅ Symbol table для отслеживания всех определений
- ✅ Валидация структуры выражений

### Runtime Engine
- ✅ Регистрация automations
- ✅ Обработка when условий
- ✅ Поддержка условий с удержанием (`for duration`)
- ✅ State machine для отслеживания состояний условий
- ✅ Заготовка для wait/for (неблокирующая реализация)
- ✅ Базовая структура для обработки событий

## 🚧 Что НЕ реализовано (TODO)

1. **Expression evaluation** - сейчас возвращает `false` (заглушка)
2. **Function calls execution** - нет подключения к Home Assistant API
3. **HTTP actions** - нет HttpClientFactory и обработки `http.telegram.post`
4. **Event normalization** - нет StateChangedEvent, CommandEvent, ScheduleEvent
5. **RabbitMQ поддержка** - нет multi-replica
6. **Type checking** - базовая проверка типов не реализована
7. **Декораторы runtime логика** - `@mode`, `@cooldown`, `@lock`, `@edge`, `@max_age`, `@debounce` парсятся, но не обрабатываются
8. **Wait/for реализация** - зарегистрирована структура, но не реализована логика ожидания
9. **Home Assistant WebSocket API** - нет интеграции

## 🛠️ Разработка

### Запуск тестов

```powershell
docker run --rm -v "${PWD}:/workspace" -w /workspace mcr.microsoft.com/dotnet/sdk:8.0 `
    dotnet test HassLanguage.sln
```

### Форматирование кода

Проект использует CSharpier для форматирования. Конфигурация в `.csharpierrc.yaml`.

## 📚 Примеры

Смотрите папку `examples/`:
- `basic-example.hass` - минимальный пример
- `simple-example.hass` - простые автоматизации
- `complex-example.hass` - сложные сценарии с комбинированными условиями

Подробнее в [README_EXAMPLES.md](README_EXAMPLES.md).

## 🏗️ Архитектура

```
Parser (Sprache) → AST → Semantic Validator → Runtime Engine
                                              ↓
                                    Event Loop + State Machines
```

### Поток выполнения

1. **Парсинг** - `HassLanguageParser.Parse()` преобразует текст в AST
2. **Валидация** - `SemanticValidator.Validate()` проверяет корректность
3. **Регистрация** - `AutomationEngine.RegisterAutomation()` регистрирует автоматизации
4. **Обработка событий** - `AutomationEngine.ProcessEvent()` обрабатывает события

## 📄 Лицензия

[Укажите лицензию, если есть]

---

**Примечание**: Проект в активной разработке. Многие функции парсятся и валидируются, но runtime логика еще не полностью реализована. См. раздел "Что НЕ реализовано" выше.
